open Board.GameBoard

module Game = struct

type board = Board.t

(*Just so shit runs, not implemented yet*)
let setup_game () = Board.instantiate_board ()

(*Just so shit runs, not implemented yet*)
let play a = a

end